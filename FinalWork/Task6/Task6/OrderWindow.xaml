<Window x:Class="Task6.OrderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Task6"
        mc:Ignorable="d"
        Title="OrderWindow" Height="450" Width="800">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5">
            <TextBlock Text="Заказ №: " FontSize="14" FontWeight="Bold"/>
            <TextBlock x:Name="OrderIdTextBlock" FontSize="14" FontWeight="Bold"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="Клиент: " FontSize="14"/>
            <TextBlock x:Name="ClientNameTextBlock" FontSize="14" FontWeight="SemiBold" Foreground="DarkSlateGray"/>
        </StackPanel>

        <DataGrid Grid.Row="2" x:Name="CartGrid" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Product.Id}" Width="50" IsReadOnly="True"/>
                <DataGridTextColumn Header="Наименование" Binding="{Binding Product.Name}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Производитель" Binding="{Binding Product.Manufacturer}" Width="130" IsReadOnly="True"/>
                <DataGridTextColumn Header="Описание" Binding="{Binding Product.Description}" Width="150" IsReadOnly="True"/>
                <DataGridTextColumn Header="Цена за единицу" Binding="{Binding Product.Price, StringFormat={}{0:N2} руб.}" Width="110" IsReadOnly="True"/>

                <DataGridTemplateColumn Header="Количество" Width="90">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBox Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" 
                         TextChanged="QuantityTextBox_TextChanged" HorizontalContentAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Стоимость" Binding="{Binding TotalItemPrice, StringFormat={}{0:N2} руб.}" Width="100" IsReadOnly="True"/>

                <DataGridTemplateColumn Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Удалить" Click="DeleteButton_Click" Background="#FFCDD2" BorderBrush="#D32F2F"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="3" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Итоговая сумма заказа: " FontSize="16" FontWeight="Bold"/>
                <TextBlock x:Name="TotalPriceTextBlock" FontSize="16" FontWeight="Bold" Foreground="#2E7D32"/>
            </StackPanel>

            <Button Grid.Column="1" Content="Оформить заказ" Click="CheckoutButton_Click" 
                    Padding="25,12" FontSize="14" FontWeight="Bold" Background="#E8F5E9" BorderBrush="#4CAF50"/>
        </Grid>
    </Grid>
</Window>
